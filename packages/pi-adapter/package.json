{
  "name": "@arp/pi-adapter",
  "version": "0.0.1",
  "type": "module",
  "main": "./dist/pi-adapter/src/review-submit.js",
  "types": "./dist/pi-adapter/src/review-submit.d.ts",
  "bin": {
    "arp-bus-worker": "./dist/pi-adapter/src/bus-worker.js",
    "arp-bus-worker-loop": "./dist/pi-adapter/src/bus-worker-loop.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist",
    "dev": "tsc -p tsconfig.json --watch",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@arp/protocol": "workspace:*",
    "@arp/domain": "workspace:*",
    "@arp/store-sqlite": "workspace:*"
  }
}
