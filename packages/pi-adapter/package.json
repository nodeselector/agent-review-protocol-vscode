{
  "name": "@arp/pi-adapter",
  "version": "0.0.1",
  "type": "module",
  "main": "../../dist/pi-adapter/src/index.js",
  "types": "../../dist/pi-adapter/src/index.d.ts",
  "bin": {
    "arp-pi-adapter": "../../dist/pi-adapter/src/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist",
    "dev": "tsc -p tsconfig.json --watch",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@arp/protocol": "workspace:*"
  }
}
