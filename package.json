{
  "name": "agent-review-protocol-vscode",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "packageManager": "pnpm@11.0.3",
  "scripts": {
    "build": "pnpm -r build",
    "clean": "pnpm -r clean && rm -rf dist",
    "dev": "pnpm -r --parallel dev",
    "test": "pnpm build && node --test tests/**/*.test.js",
    "typecheck": "pnpm -r typecheck",
    "qa": "pnpm typecheck && pnpm test"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.8.3"
  }
}
